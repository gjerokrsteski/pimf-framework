<nav>
    <ul>
      <?php
      foreach($this->entries as $entry) :
      ?>
        <li>
            <h2><a href="?action=showentry&id=<?=$entry->getId()?>"><?=$entry->getTitle()?></a></h2>
        </li>
      <?php
      endforeach;
      ?>
    </ul>
</nav>